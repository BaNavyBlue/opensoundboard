#include "save_config.h"

const char ws[5] = "    ";

bool save_config(std::vector<BUTTON> saveButtons, std::string fileName)
{
    std::string outTxt;
    for(unsigned i = 0; i < saveButtons.size(); ++i){
        
    }
    return true;
}